// This file is not currently used by the app entry point, but it has been cleaned up
// so it no longer contains invalid React syntax.

import React from 'react';

export default function IndexHelp() {
  return (
    <div className="info">
      <h2>Campus Notifications</h2>
      <p>
        The notifications fetch and display logic is implemented in <code>src/App.js</code>.
      </p>
    </div>
  );
}
